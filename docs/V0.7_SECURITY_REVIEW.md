# v0.7 focused security review handoff

Baseline: `fea081a`, branch `codex/v0.7-local-web-ui`. The owner narrowed the
review on 2026-09-14 to essential fixes and local checks; remaining standalone
and hosted work is deferred to the next agent. This is **not** release approval.

## Fixed

- P1: HTTP workers were unbounded, inactivity timeouts could be extended by
  trickling bytes, and shutdown waited for request threads before cancelling
  verifier jobs. Bound connections to 32, enforce a 30-second absolute connection
  deadline, interrupt active sockets on close, and cancel jobs before draining.
- P1: Uploads held the global job lock while reading the network, blocking job
  operations and shutdown. Reserve file count/bytes under the lock, perform I/O
  outside it, and cap session uploads at 2 GiB including in-flight reservations.
- P2: Ambiguous framing/duplicate security headers and unread bodies could be
  reused as subsequent requests. Reject duplicate/folded security headers,
  Transfer-Encoding, Expect and GET bodies; serve one request per connection.
  Validate supplied Origin on GET as well as POST.
- P2: Artifact registration resolved symlinks before rejecting them; downloads
  accepted equal-length in-place modifications and could block on a raced FIFO.
  Reject links before resolution, compare descriptor/name identities and file
  timestamps, reject hardlinks/non-regular files, open nonblocking/no-follow where
  supported, and never send beyond the registered Content-Length.
- P2: Configuration size checks preceded an unbounded reopen/read. The shared
  parser now reads at most 1 MiB + 1; Web imports pass a verified open descriptor.
- P2: Standalone JAR rejection happened after archive/checksum creation, and
  recursive Web resource collection could include unrelated local files.
  Resource collection now explicitly selects the three UI assets; an audit
  rejects runtime/private filenames, special files and escaping links before
  archive creation. Internal native-library symlinks remain supported.

Regression tests cover raw HTTP framing/pipelining, partial-header deadlines and
shutdown, worker capacity, upload locking/quota, artifact mutation/links and
configuration descriptor consumption. Runtime verdict and Provider logic are unchanged.

## Local validation

- One full offline run: 182 tests, 6 skips, 1 failure in the new Windows
  partial-header shutdown regression. All other tests passed.
- Fixed that failure by closing the detached native socket after shutdown;
  Windows timed reads were not reliably interrupted by shutdown alone.
- Final focused Web run: 14 tests, 13 passed, 1 symlink-permission skip.
  No second full run was performed, per the owner's narrowed scope.
- Follow-up focused tests: Web 14 passed / 1 skipped; standalone asset tests
  2 passed / 1 skipped. The new Web test starts a real owned OS process and
  confirms application close cancels the job, drains the process and frees slots.
- `python -m compileall -q pluginmatrix tests standalone` and `git diff --check`
  passed.
- Windows PyInstaller build/archive and frozen CLI/Provider/Java/Javac/Web
  health smoke passed with installed JDK 17. The first smoke requested Java 21
  and correctly rejected the available JDK 17; rerun used `--java 17`.
  Development zip SHA-256:
  `1bc742d164bed3605d20e5124c34375ec949d24bb4c911e6e321637e1ddddf1a`.
  It was built from the uncommitted follow-up source and is not a release asset.
  Hosted gates remain pending.

## Required continuation

- Run the changed candidate through hosted offline CI (especially POSIX symlink
  behavior) and the existing four-target standalone workflow with frozen Web /
  real Paper checks. These gates were deliberately not repeated locally.
- Finish standalone asset isolation/license review. The new filename/path audit
  is not a content scan for secrets or embedded build-machine paths, nor a
  complete dependency/license inventory. The vendored CPython fallback is
  the core LICENSE, not a complete inventory of licenses for bundled native
  dependencies. Verify actual per-platform binaries and notices before release.
- Review the frozen Windows DLL lock scope: `run_external` holds the global
  override across the entire subprocess wait (including up to 60-second javac),
  which serializes concurrent launches. No change to that helper is included.
- No tag, release, PyPI publication, main merge, new version, or full Provider
  Gate was performed. GPT-5.6 Sol can continue the Release Gate, but must resolve
  the pending items rather than treat this handoff as a passed security gate.

## Continuation result

Completed on 2026-09-14 at candidate
`d3aeecc1b20d26b22ab9a9d75ff744d7a57a9b42`.

- The post-fix full offline suite passed twice on Windows Python 3.11: 186
  tests, 7 POSIX/symlink-permission skips. Final `compileall`, package audit,
  `git diff --check`, tracked/untracked source inspection and clean push passed.
- Final hosted CI run
  [34831078163](https://github.com/YouDaoRS/PluginMatrix/actions/runs/34831078163)
  passed Ubuntu/Windows and Python 3.10/3.11. Every job ran 186 tests; Linux
  had 2 Windows-only skips and Windows had 3 POSIX-only skips.
- The first post-security standalone run was functionally green, but archive
  inspection confirmed that Unix bundles copied OpenSSL and other native
  libraries while the fallback `PYTHON-LICENSE.txt` contained only the CPython
  core license. Commit `d3aeecc` adds `NATIVE-LICENSES.txt`, lists all license
  files in build provenance, and adds packaging regressions.
- Final standalone run
  [34831078226](https://github.com/YouDaoRS/PluginMatrix/actions/runs/34831078226)
  passed Windows x86-64, Linux x86-64, macOS x86-64 and macOS arm64. Frozen
  CLI and Web paths each returned a real Paper PASS with probe evidence and
  retained JSON/HTML/server.log artifacts on every target.
- Downloaded archives matched their per-platform and combined checksums. They
  contained no JARs, runtime logs/caches, credentials, private-key formats,
  environment files or build-machine paths. Four internal symlinks in each
  macOS bundle remained inside the native-library directory. All bundled
  notice links returned HTTP 200.
- Provider and Runtime Verifier behavior was unchanged, so the completed real
  Provider Gate was not repeated. The frozen Windows global DLL-environment
  lock can still serialize concurrent external launches; this is a bounded
  performance limitation, not an observed verdict or cleanup failure.

The focused security continuation is closed. The candidate may enter the v0.7
final Release Gate; it has not been tagged, released, published or merged.
